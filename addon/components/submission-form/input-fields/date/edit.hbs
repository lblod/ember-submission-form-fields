<div class="au-o-grid au-o-grid--flush">
  <div class="au-o-grid__item au-u-2-4">
    <AuLabel class="{{if this.errors "au-c-label--error" ""}}"
      for={{this.inputId}}>
      {{@field.label}}
    </AuLabel> {{!-- setting ID's (to link the label to the input field) does not work with the datepicker plugin --}}
  </div>
  <div class="au-o-grid__item au-u-1-4 au-u-text-right">
    {{#if this.isRequired}}
      <AuPill>Verplicht</AuPill>
    {{/if}}
  </div>
</div>
<div class="au-o-grid au-o-grid--flush">
  <div class="au-o-grid__item au-u-3-4">
    <WuDatepicker
            class="au-c-date-picker-container {{if this.errors "au-c-date-picker-container--error" ""}} {{if this.save.isRunning "au-c-input--disabled"}} {{if this.submit.isRunning "au-c-input--disabled"}} {{if this.delete.isRunning "au-c-input--disabled"}}"
            id="{{this.inputId}}"
            @width={{"block"}}
            @tagName="div"
            @dateFormat="DD.MM.YYYY"
            @onChange={{this.updateValue}}
            @value={{this.value}}
    />
  </div>
</div>

<AuHelpText class="{{if this.errors "au-c-help-text--error" ""}}">Datum formaat: DD.MM.JJJJ</AuHelpText>
{{#each this.errors as |error|}}
  <AuHelpText @error={{"error"}}>{{error.resultMessage}}</AuHelpText>
{{/each}}

