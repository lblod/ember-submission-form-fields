<AuLabel for={{this.inputId}}>
  {{@field.label}}
</AuLabel>
<PowerSelect
  @triggerId={{this.inputId}}
  @disabled={{true}}
  @selected={{this.selected}}
  @options={{this.options}}
  @onChange={{this.noop}}
  as |concept|
>
  {{concept.label}}
</PowerSelect>