<AuLabel class="{{if this.errors "au-c-label--error"}}" for={{this.inputId}}>
  {{@field.label}}
  {{#if this.isRequired}}
    <AuPill>Verplicht</AuPill>
  {{/if}}
</AuLabel>
<AuTextarea
  id="{{this.inputId}}"
  @width={{"block"}}
  disabled={{true}}
  @value={{this.value}}
/>
