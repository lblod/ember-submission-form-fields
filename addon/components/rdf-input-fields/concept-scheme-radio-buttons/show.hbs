<AuLabel class="{{if this.errors "au-c-label--error"}}" for={{this.inputId}}>
  {{@field.label}}
  {{#if this.isRequired}}
    <AuPill>Verplicht</AuPill>
  {{/if}}
</AuLabel>
<ul>
  {{#each this.options as |option|}}
    <li>
      <AuControlRadio @identifier="{{this.inputId}}"
                      @name={{@field.uri}}
                      @label={{option.label}}
                      @value={{option.label}}
                      @checked={{if (eq this.value option.value) true false}}
                      disabled={{true}}/>
    </li>
  {{/each}}
</ul>


