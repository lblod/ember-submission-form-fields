<AuLabel for={{this.inputId}}>
  {{@field.label}}
</AuLabel>
<PowerSelectMultiple
  @triggerId={{this.inputId}}
  @disabled={{true}}
  @selected={{this.selected}}
  @options={{this.subset}}
  @onChange={{this.noop}}
  as |concept|
>
  {{concept.label}}
</PowerSelectMultiple>